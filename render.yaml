services:
  - type: web
    name: vital-ativa-backend
    env: java
    plan: free
    region: oregon
    buildCommand: bash build.sh
    startCommand: java -jar backend/target/vital-ativa-backend-1.0.0.jar
    autoDeploy: true